# Production
# Replace with your actual Render backend URL (no trailing slash).
# You can also override this in the Vercel dashboard under
# Settings → Environment Variables → VITE_API_URL
VITE_API_URL=https://your-backend.onrender.com
