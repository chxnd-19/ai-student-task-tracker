# ── API Base URL ──────────────────────────────────────────────────────────────
# Development: leave empty — Vite proxy forwards /api/* to localhost:5000
# Production:  set to your deployed backend URL (no trailing slash)
VITE_API_URL=http://localhost:5000
